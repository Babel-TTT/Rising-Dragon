//This file sets all vehicle IDs. Each type has a range of 32/64/128/256/512/768/1024 possible IDs.
//Mirror of China-Set-Buses: every vehicle gets one item slot reserved here so that the
//in-game vehicle IDs stay stable across releases, independent of source-file order.
//
//No vehicles yet — as soon as the first vehicle exists, reserve a block like:
//
//  //某类单机（1000-1999）
//  item (FEAT_ROADVEHS, MyFirstVehicle, 1000) {}
//
//and then define that item in its own file (see the #include block in military-pack.pnml).
